<x-layouts.app title="Odeslat upomínku"><h1 class="text-3xl font-bold">Odeslat upomínku</h1><div class="card mt-6"><p>Faktura <strong>{{ $invoice->document_number }}</strong></p><p class="text-sm text-slate-600">Příjemce: {{ $invoice->issuedRevision->customerSnapshot->email ?: 'chybí e-mail' }}</p><form method="POST" action="{{ route('invoices.reminders.send',$invoice->uuid) }}" class="mt-5 space-y-4" x-data="{ level: '1' }">@csrf<label>Stupeň<select class="input mt-1" name="level" x-model="level"><option value="1">1. upomínka</option><option value="2">2. upomínka</option><option value="3">3. upomínka</option></select></label>@foreach($previews as $level=>$preview)<div x-show="level === '{{ $level }}'" class="rounded border bg-slate-50 p-4"><p class="font-semibold">{{ $preview['subject'] }}</p><p class="mt-2 whitespace-pre-line text-sm">{{ $preview['body'] }}</p></div>@endforeach<p class="text-sm text-slate-500">Již evidovaný stupeň nebude odeslán podruhé.</p><button class="btn-primary">Potvrdit odeslání</button></form></div></x-layouts.app>
