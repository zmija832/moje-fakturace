<x-layouts.app title="Opakované faktury">
<div class="flex items-center justify-between"><div><p class="text-sm font-medium text-blue-700">Automatizace</p><h1 class="text-3xl font-bold">Opakované faktury</h1></div>@can('create',App\Models\Business\RecurringInvoiceTemplate::class)<a class="btn-primary" href="{{ route('recurring.create') }}">Nová šablona</a>@endcan</div>
<div class="card mt-6 overflow-x-auto"><table class="w-full text-left text-sm"><thead><tr class="border-b"><th class="p-3">Název</th><th>Periodicita</th><th>Další běh</th><th>Režim</th><th>Stav</th><th></th></tr></thead><tbody>@forelse($templates as $template)<tr class="border-b"><td class="p-3 font-semibold">{{ $template->name }}</td><td>{{ [1=>'Měsíčně',3=>'Čtvrtletně',6=>'Pololetně',12=>'Ročně'][$template->interval_months] ?? $template->interval_months.' měsíců' }}</td><td>{{ $template->next_run_on->format('d. m. Y') }}</td><td>{{ $template->mode==='draft'?'Koncept':'Automaticky vystavit' }}</td><td>{{ $template->is_active?'Aktivní':'Pozastavená' }}</td><td><a class="text-blue-700" href="{{ route('recurring.show',$template->uuid) }}">Detail</a></td></tr>@empty<tr><td colspan="6" class="p-6 text-center text-slate-500">Zatím nejsou vytvořené žádné šablony.</td></tr>@endforelse</tbody></table></div><div class="mt-4">{{ $templates->links() }}</div>
</x-layouts.app>
