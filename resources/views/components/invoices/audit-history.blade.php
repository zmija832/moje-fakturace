@props(['audits'])
<section class="card mt-6"><h2 class="text-lg font-bold">Auditní historie</h2><p class="mt-1 text-sm text-slate-600">Posledních bezpečně sanitizovaných událostí této faktury.</p>@if($audits->isEmpty())<p class="mt-4 text-sm">Zatím bez záznamů.</p>@else<ol class="mt-4 divide-y divide-slate-200">@foreach($audits as $audit)<li class="py-3 text-sm"><div class="flex flex-wrap justify-between gap-2"><span class="font-semibold">{{ $audit->event->label() }}</span><time datetime="{{ $audit->occurred_at->toIso8601String() }}">{{ $audit->occurred_at->format('j. n. Y H:i:s') }}</time></div><p class="mt-1 text-slate-600">{{ $audit->actor_name ?? 'Systém' }}</p></li>@endforeach</ol>@endif</section>
